# 智策通 V1.0 细分开发任务清单

## 一、文档定位

本文档基于《智策通_技术选型与开发规划.md》进一步拆解，目标是把“技术方案”转化为可开发、可排期、可验收的细分子任务。

V1.0 的核心目标：

> 做出一个可运行、可演示、可解释、可追溯的高校政策智能助手 Demo。

V1.0 的核心闭环：

```text
管理员上传政策文件
→ 系统解析文件并构建分层政策知识库
→ 学生自然语言提问
→ RAG 检索政策依据
→ Agent 读取记忆并追问缺失条件
→ 判断资格
→ 生成材料清单与办理流程
→ 输出出处、风险提示和下一步建议
```

## 二、任务拆解原则

1. **先闭环，后增强**
   优先完成上传、解析、入库、检索、问答、引用这条主链路。

2. **先高校，后多域**
   V1.0 聚焦西安电子科技大学高校政策，城市青年、企业、政务服务作为后续扩展。

3. **先文本型 PDF，后复杂文档**
   第一阶段优先支持文本型 PDF 和 Word，扫描件 OCR、复杂表格解析放到后续。

4. **先轻量 Agent，后 LangGraph**
   V1.0 用轻量 Python 编排保证稳定，流程成熟后再升级 LangGraph。

5. **先结构化记忆，后长期检索记忆**
   V1.0 用 PostgreSQL 存短期记忆、事项记忆和长期记忆条目，Redis 和长期记忆检索放到后续。

6. **政策 RAG 必须带治理能力**
   政策知识库要支持层级、适用范围、有效时间、主附件关系和出处引用。

## 三、里程碑总览

| 里程碑 | 阶段目标 | 主要交付物 |
|---|---|---|
| M0 | 需求冻结与资料准备 | Demo 范围、政策文件、演示脚本初稿 |
| M1 | 基础工程搭建 | Next.js、FastAPI、PostgreSQL + pgvector、Docker Compose |
| M2 | 数据库与数据模型 | 政策、RAG、记忆、会话、管理后台核心表 |
| M3 | 文件上传与解析 | 文件上传、PDF/Word 解析、metadata 录入、附件关系 |
| M4 | RAG 知识库构建 | 文档切片、向量化、混合检索、出处引用 |
| M5 | 政策智能问答 | 问答接口、问答页面、引用展示、热门问题记录 |
| M6 | Agent 与记忆 | 意图识别、追问、资格判断、流程生成、记忆读写 |
| M7 | 管理后台与看板 | 文件管理、条款查看、标准答案、热门问题 |
| M8 | Demo 打磨与验收 | 稳定演示案例、前端优化、测试、答辩素材 |

## 四、M0：需求冻结与资料准备

### M0.1 明确 V1.0 演示范围

任务：

- 确定 V1.0 只做高校政策场景。
- 确定高校样板为西安电子科技大学。
- 确定首批事项：奖学金、转专业、毕业要求。
- 确定辅助事项：助学金、保研、请假、处分、学籍管理。
- 确定第一版用户：学生、管理员。
- 辅导员和教务老师先作为扩展角色，不做复杂权限。

验收标准：

- 形成 V1.0 范围说明。
- 明确哪些功能必须做，哪些功能暂缓。

### M0.2 收集政策文件

任务：

- 收集 3 到 5 份西电政策文件。
- 优先收集文本型 PDF 或 Word。
- 文件建议包括：
  - 学生手册
  - 奖学金评定办法
  - 转专业通知
  - 毕业要求文件
  - 学籍管理办法
- 整理文件标题、发布部门、发布时间、生效时间。

验收标准：

- 至少准备 3 份可解析政策文件。
- 每份文件有基础 metadata。

### M0.3 准备演示问题集

任务：

- 奖学金问答案例：
  - “我挂过一门课，还能申请奖学金吗？”
  - “奖学金申请需要满足什么条件？”
- 转专业资格判断案例：
  - “我大一绩点 3.4，想转计算机专业，可以吗？”
  - “转专业需要准备哪些材料？”
- 毕业要求案例：
  - “我学分不够会影响毕业吗？”

验收标准：

- 至少准备 10 个演示问题。
- 标记其中 3 个作为路演稳定案例。

## 五、M1：基础工程搭建

### M1.1 初始化项目目录

任务：

- 创建统一项目根目录。
- 建立前端目录 `frontend/`。
- 建立后端目录 `backend/`。
- 建立数据库、文档、脚本目录。
- 编写基础 `README.md`。

建议目录：

```text
zhicetong/
  frontend/
  backend/
  docs/
  scripts/
  docker-compose.yml
  README.md
```

验收标准：

- 目录结构清晰。
- 前后端可以独立启动。

### M1.2 初始化 Next.js 前端

任务：

- 创建 Next.js + TypeScript 项目。
- 配置 Tailwind CSS。
- 配置 shadcn/ui。
- 配置 lucide-react。
- 建立基础布局。
- 建立路由：
  - `/`
  - `/chat`
  - `/eligibility`
  - `/cases`
  - `/policies`
  - `/admin`

验收标准：

- 前端项目能正常启动。
- 首页能访问。
- 基础导航可用。

### M1.3 初始化 FastAPI 后端

任务：

- 创建 FastAPI 项目。
- 配置 Pydantic。
- 配置 SQLAlchemy。
- 配置 Uvicorn。
- 建立基础目录：
  - `api/`
  - `core/`
  - `db/`
  - `models/`
  - `schemas/`
  - `services/`
  - `prompts/`
- 实现健康检查接口。

接口：

```text
GET /api/health
```

验收标准：

- 后端可启动。
- `/api/health` 返回正常状态。

### M1.4 配置 PostgreSQL + pgvector

任务：

- 编写 Docker Compose。
- 启动 PostgreSQL。
- 安装 pgvector 扩展。
- 配置数据库连接字符串。
- 后端连接数据库。
- 编写数据库初始化脚本。

验收标准：

- PostgreSQL 容器可启动。
- pgvector 扩展可用。
- 后端能连接数据库。

### M1.5 配置环境变量

任务：

- 前端配置 `.env.local`。
- 后端配置 `.env`。
- 配置数据库连接。
- 配置文件上传目录。
- 配置模型 API Key 占位。
- 配置 embedding 模型名称占位。

验收标准：

- 本地环境变量可读取。
- 敏感信息不写入代码。

## 六、M2：数据库与数据模型

### M2.1 设计用户与会话表

任务：

- 设计 `users` 表。
- 设计 `chat_sessions` 表。
- 设计 `chat_messages` 表。
- V1.0 可使用匿名演示用户。

字段建议：

```text
users:
  id
  role
  display_name
  created_at

chat_sessions:
  id
  user_id
  title
  current_case_id
  created_at
  updated_at

chat_messages:
  id
  session_id
  role
  content
  metadata
  created_at
```

验收标准：

- 能创建会话。
- 能保存用户消息和系统回答。

### M2.2 设计政策文件表

任务：

- 设计 `policy_documents`。
- 设计 `policy_attachments`。
- 设计 `policy_relations`。
- 设计 `policy_versions`。
- 设计 `policy_scopes`。

重点字段：

```text
policy_documents:
  id
  title
  file_name
  file_path
  file_type
  policy_level
  policy_category
  issuing_department
  applicable_scope
  college
  publish_date
  effective_from
  effective_to
  version
  parse_status
  created_at
```

验收标准：

- 可以保存学校通用政策、部门政策、学院政策和附件信息。
- 可以表达主文件与附件关系。

### M2.3 设计政策切片表

任务：

- 设计 `policy_chunks`。
- 支持向量字段。
- 支持全文检索字段。
- 支持 metadata 字段。

字段建议：

```text
policy_chunks:
  id
  document_id
  attachment_id
  chunk_text
  chunk_index
  section_title
  article_no
  page_no
  policy_level
  policy_category
  applicable_scope
  effective_from
  effective_to
  metadata
  embedding
  created_at
```

验收标准：

- 每个 chunk 能追溯到具体文件、附件、页码或条款。
- 向量字段可用于 pgvector 检索。

### M2.4 设计记忆表

任务：

- 设计 `memory_items`。
- 设计 `service_cases`。
- 设计 `case_slots`。
- 明确短期记忆、事项记忆、长期记忆的存储方式。

字段建议：

```text
memory_items:
  id
  user_id
  session_id
  case_id
  memory_scope
  memory_type
  key
  value
  source
  confidence
  expires_at
  created_at
  updated_at

service_cases:
  id
  user_id
  session_id
  case_type
  case_title
  status
  result_summary
  created_at
  updated_at

case_slots:
  id
  case_id
  slot_key
  slot_name
  value
  status
  question
  required
  created_at
  updated_at
```

验收标准：

- 能保存当前事项缺失条件。
- 能保存长期记忆中的用户画像类信息。
- 用户画像不单独成层，而是长期记忆中的 `profile` 类型。

### M2.5 设计问答引用与管理表

任务：

- 设计 `citations`。
- 设计 `standard_answers`。
- 设计 `hot_questions`。
- 设计 `eligibility_records`。

验收标准：

- 回答可以关联多个政策出处。
- 后台可以维护标准答案。
- 可以统计热门问题。

## 七、M3：政策文件上传与解析

### M3.1 实现文件上传接口

任务：

- 实现上传接口。
- 限制文件类型：PDF、DOCX。
- 限制文件大小。
- 保存文件到本地目录。
- 写入 `policy_documents`。

接口：

```text
POST /api/documents/upload
```

验收标准：

- 管理员可以上传文件。
- 上传后数据库有文件记录。
- 文件保存在本地目录。

### M3.2 实现 metadata 录入

任务：

- 上传时填写或后续编辑 metadata。
- 字段包括：
  - 政策层级
  - 政策类别
  - 发布部门
  - 适用范围
  - 学院
  - 发布时间
  - 生效时间
  - 失效时间
  - 版本
- 前端提供表单。

验收标准：

- 每份政策文件有可编辑 metadata。
- metadata 能参与后续检索过滤。

### M3.3 实现主文件与附件关系

任务：

- 上传文件时可选择“主文件”或“附件”。
- 附件可选择所属主文件。
- 写入 `policy_attachments`。
- 写入 `policy_relations`。

关系类型：

```text
has_attachment
attachment_of
```

验收标准：

- 能表示“通知”与“附件 1 / 附件 2”的关系。
- 附件内容能被单独解析和检索。

### M3.4 实现 PDF 解析

任务：

- 使用 PyMuPDF 解析文本型 PDF。
- 按页提取文本。
- 保留页码。
- 保存解析文本。
- 标记解析状态。

验收标准：

- 文本型 PDF 可以成功解析。
- 每段文本能追溯页码。

### M3.5 实现 Word 解析

任务：

- 使用 python-docx 解析 DOCX。
- 提取段落文本。
- 保留标题和段落顺序。
- 保存解析文本。

验收标准：

- DOCX 可以成功解析。
- 段落顺序基本正确。

### M3.6 实现解析状态与失败处理

任务：

- 状态包括：
  - uploaded
  - parsing
  - parsed
  - indexed
  - failed
- 解析失败时保存错误信息。
- 后台支持重新解析。

验收标准：

- 管理员能看到解析状态。
- 失败文件可以重试。

## 八、M4：RAG 知识库构建

### M4.1 实现政策切片

任务：

- 按标题、章节、条款、段落切分。
- 每个 chunk 保留上下文标题。
- 控制 chunk 长度。
- 避免切断条款。
- 对附件单独切片。

验收标准：

- 切片结果语义完整。
- chunk 能追溯文件、附件、页码、章节。

### M4.2 实现 chunk metadata 注入

任务：

- 将文件 metadata 注入 chunk。
- 注入字段：
  - policy_level
  - policy_category
  - applicable_scope
  - college
  - effective_from
  - effective_to
  - parent_document_id
  - attachment_title
- 保存到 `policy_chunks.metadata`。

验收标准：

- 每个 chunk 都有可用于过滤的 metadata。

### M4.3 实现 embedding 向量化

任务：

- 封装 embedding provider。
- 支持云端 embedding。
- 支持后续切换本地 embedding。
- 批量生成向量。
- 写入 pgvector。

验收标准：

- chunk 可生成向量。
- 向量写入数据库。

### M4.4 实现向量检索

任务：

- 基于 pgvector 实现 top-k 检索。
- 支持相似度分数。
- 支持按 metadata 过滤。

验收标准：

- 输入问题后能召回相关政策片段。
- 能按政策类别过滤。

### M4.5 实现关键词检索

任务：

- 使用 PostgreSQL 全文检索或简单关键词匹配。
- 支持政策术语命中。
- 返回关键词分数。

验收标准：

- 对“奖学金”“转专业”“挂科”等关键词能准确召回。

### M4.6 实现混合检索

任务：

- 合并向量检索结果和关键词检索结果。
- 设计简单排序规则。
- 去重。
- 返回统一结果。

排序参考：

```text
final_score = vector_score * 0.7 + keyword_score * 0.3 + authority_bonus + recency_bonus
```

验收标准：

- 混合检索结果优于单纯向量检索。
- 结果包含来源和分数。

### M4.7 实现政策分层过滤

任务：

- 支持按政策层级过滤：
  - school
  - department
  - college
  - case
  - attachment
- 支持按适用范围过滤。
- 支持按学院过滤。

验收标准：

- 学院类问题能优先召回学院政策。
- 学校通用政策仍可作为基础依据。

### M4.8 实现有效时间过滤

任务：

- 检索时过滤过期政策。
- 对无失效时间政策设置默认有效。
- 支持历史年份查询。
- 过期政策需要降权或提示。

验收标准：

- 当前问题优先使用当前有效政策。
- 过期政策不会被误当作最新依据。

### M4.9 实现附件关联召回

任务：

- 当主文件被召回时，可关联召回附件。
- 当附件被召回时，可追溯主文件。
- 回答中展示附件标题。

验收标准：

- 问“需要什么表”时能指向具体附件。
- 不只回答“见附件”，而能说清附件名称和内容。

### M4.10 构建检索测试集

任务：

- 为每类政策准备测试问题。
- 人工标注期望命中文件。
- 测试 top-3 命中率。
- 记录失败问题。

验收标准：

- 至少 20 条检索测试问题。
- top-3 命中率达到可演示水平。

## 九、M5：政策智能问答

### M5.1 实现 LLM Provider

任务：

- 封装统一模型接口。
- 支持 `generate()`。
- 支持 `embed()`。
- 支持模型配置切换。
- 支持 mock 模式。

验收标准：

- 后端不绑定单一模型厂商。
- 没有 API Key 时可用 mock 流程调试。

### M5.2 实现政策问答 Prompt

任务：

- 设计系统 Prompt。
- 要求基于检索结果回答。
- 要求引用政策来源。
- 要求区分政策依据和 AI 推断。
- 要求不确定时明确说明。

验收标准：

- 回答不脱离政策片段。
- 回答结构稳定。

### M5.3 实现问答接口

接口：

```text
POST /api/chat
```

任务：

- 接收用户问题。
- 创建或读取会话。
- 调用 RAG 检索。
- 调用 LLM 生成回答。
- 保存消息。
- 保存引用。
- 更新热门问题。

验收标准：

- 前端可正常提问。
- 后端返回回答、引用和检索片段。

### M5.4 实现引用出处

任务：

- 回答附带文件名。
- 附带附件名。
- 附带页码或条款编号。
- 附带原文片段。
- 前端可展开查看。

验收标准：

- 每个回答至少展示 1 个出处。
- 出处能回到具体政策 chunk。

### M5.5 实现问答页面

任务：

- 聊天式输入框。
- 展示用户问题。
- 展示系统回答。
- 展示引用卡片。
- 展示“政策依据”和“AI 推断”。
- 支持加载状态和错误状态。

验收标准：

- 可完成政策问答演示。
- 页面有正式产品感。

## 十、M6：Agent 编排与记忆

### M6.1 实现意图识别

任务：

- 判断用户问题类型：
  - policy_qa
  - eligibility_check
  - workflow_generation
  - material_list
  - general_chat
- 可先用规则 + LLM 分类。

验收标准：

- 能识别“问政策”和“判资格”的区别。

### M6.2 实现短期记忆

任务：

- 从 `chat_messages` 读取当前会话上下文。
- 提取本轮关键信息。
- 在当前会话中使用。
- 避免重复追问。

验收标准：

- 用户已经回答过的信息，系统不会马上重复问。

### M6.3 实现事项记忆

任务：

- 创建 `service_case`。
- 根据事项生成 required slots。
- 保存已知条件。
- 标记 missing slots。

转专业 slots 示例：

```text
grade
gpa
target_major
has_failed_course
has_disciplinary_record
application_period
target_college_requirement
```

验收标准：

- 系统知道当前正在判断哪个事项。
- 系统知道还缺哪些条件。

### M6.4 实现长期记忆

任务：

- 将稳定信息写入 `memory_items`。
- 用户画像作为长期记忆中的 `profile` 类型。
- 历史咨询作为长期记忆中的 `history` 类型。
- 常办事项作为长期记忆中的 `preference` 或 `behavior` 类型。

示例：

```json
{
  "memory_scope": "long_term",
  "memory_type": "profile",
  "key": "grade",
  "value": "大一"
}
```

验收标准：

- 长期记忆可以保存用户稳定属性。
- 用户画像不作为单独一层，而是长期记忆的结构化内容。

### M6.5 实现信息缺口判断

任务：

- 根据政策事项判断必要字段。
- 对照短期记忆、事项记忆、长期记忆。
- 找出缺失字段。
- 生成追问问题。

验收标准：

- 用户问“我能转专业吗”时，系统能主动追问绩点、挂科、年级等条件。

### M6.6 实现资格判断

任务：

- 输入政策条款、用户条件和事项记忆。
- 输出：
  - 符合条件
  - 不符合条件
  - 待确认条件
  - 综合判断
  - 政策依据
- 结果写入 `eligibility_records`。

验收标准：

- 能对奖学金或转专业进行初步资格判断。
- 判断中有政策依据。

### M6.7 实现材料清单生成

任务：

- 从政策条款中抽取材料要求。
- 结合附件信息。
- 生成材料清单。
- 标记必需材料和可选材料。

验收标准：

- 能输出“需要准备哪些材料”。
- 如果材料来自附件，能指向具体附件。

### M6.8 实现办理流程生成

任务：

- 根据政策条款生成步骤。
- 包括时间、部门、提交方式、审核流程。
- 信息缺失时标记待确认。

验收标准：

- 能输出 3 到 8 步办理流程。
- 流程中包含注意事项。

### M6.9 实现风险校验

任务：

- 检查回答是否有引用。
- 检查是否使用过期政策。
- 检查是否存在学校政策和学院政策冲突。
- 检查是否缺少必要条件。
- 输出风险等级：
  - low
  - medium
  - high

验收标准：

- 不确定问题会明确提示。
- 高风险问题不下绝对结论。

### M6.10 实现 Agent Orchestrator

任务：

- 串联：
  - 读取记忆
  - 意图识别
  - RAG 检索
  - 信息缺口判断
  - 追问或判断
  - 流程生成
  - 风险校验
  - 更新记忆
- 输出统一响应格式。

验收标准：

- 奖学金问答和转专业判断能跑通完整 Agent 流程。

## 十一、M7：管理后台与看板

### M7.1 实现政策文件管理页

任务：

- 文件列表。
- 上传入口。
- 解析状态。
- 文件 metadata 编辑。
- 删除或禁用文件。

验收标准：

- 管理员能管理政策文件。

### M7.2 实现政策条款查看页

任务：

- 按文件查看 chunk。
- 显示页码、章节、metadata。
- 支持搜索。
- 支持查看附件 chunk。

验收标准：

- 管理员能检查知识库内容。

### M7.3 实现标准答案维护

任务：

- 创建标准答案。
- 绑定政策类别或问题关键词。
- 标记适用范围。
- 管理员可编辑。

验收标准：

- 高频问题可以有人工维护答案。

### M7.4 实现热门问题看板

任务：

- 统计问题次数。
- 按政策类别聚合。
- 展示热门事项。
- 展示最近咨询趋势。

验收标准：

- 后台能展示管理价值。

### M7.5 实现基础数据看板

指标：

- 已上传政策数量
- 已解析文件数量
- 政策 chunk 数量
- 今日问答次数
- 热门政策类别
- 高风险回答数量

验收标准：

- 路演时可以展示平台运营能力。

## 十二、M8：前端页面细化

### M8.1 政策服务工作台首页

模块：

- 顶部导航
- 主输入框
- 快捷入口：
  - 我要问政策
  - 我要判断资格
  - 我要办理事项
  - 管理入口
- 事项卡片：
  - 奖学金
  - 助学金
  - 转专业
  - 保研
  - 毕业
  - 请假
  - 处分
  - 学籍
- 可信说明

验收标准：

- 首屏不是单纯聊天框。
- 评委能一眼理解产品价值。

### M8.2 智能问答页

模块：

- 会话列表
- 消息区
- 输入框
- 引用依据卡片
- 政策原文展开
- 风险提示

验收标准：

- 能完成政策问答主流程。

### M8.3 资格判断页

模块：

- 当前事项状态
- 已知条件
- 待补充条件
- Agent 追问区
- 判断结果
- 政策依据
- 材料清单
- 办理流程

验收标准：

- 能展示“我是否符合条件”的完整过程。

### M8.4 事项中心页

模块：

- 事项分类
- 事项详情
- 常见问题
- 相关政策
- 开始办理或开始判断按钮

验收标准：

- 用户可以按事项而非只按聊天入口使用系统。

### M8.5 管理后台页面

模块：

- 文件上传
- 文件管理
- 条款查看
- 热门问题
- 标准答案
- 数据看板

验收标准：

- 能体现系统可运营、可落地。

## 十三、测试任务

### T1 单元测试

覆盖：

- 文档解析
- 文档切片
- metadata 注入
- 向量检索
- 关键词检索
- 记忆读写
- 意图识别

验收标准：

- 关键服务函数有基础测试。

### T2 接口测试

覆盖：

- 上传文件
- 获取文件列表
- 解析文件
- 构建索引
- 问答接口
- 资格判断接口
- 热门问题接口

验收标准：

- 核心 API 可通过接口测试。

### T3 RAG 检索测试

任务：

- 准备 20 条政策问题。
- 标注期望政策文件。
- 检查 top-3 命中。
- 记录失败样例。

验收标准：

- 演示问题必须稳定命中正确政策。

### T4 Demo 全链路测试

任务：

- 从空数据库开始上传政策。
- 解析并入库。
- 提问奖学金问题。
- 提问转专业问题。
- 完成追问和资格判断。
- 查看后台热门问题。

验收标准：

- 演示链路连续运行无阻塞。

## 十四、V1.0 验收标准

### 必须达成

1. 系统可以本地启动。
2. 前端有政策服务工作台。
3. 管理员可以上传政策文件。
4. 系统可以解析文本型 PDF。
5. 系统可以构建 RAG 知识库。
6. 用户可以自然语言提问。
7. 回答带政策出处。
8. 系统能进行至少一个事项的资格判断。
9. 系统能生成材料清单和办理流程。
10. 系统能保存短期记忆、事项记忆和长期记忆条目。
11. 后台能展示政策文件和热门问题。

### 建议达成

1. 支持 Word 解析。
2. 支持附件关系。
3. 支持政策有效时间过滤。
4. 支持学校政策和学院政策层级标识。
5. 支持标准答案维护。
6. 支持基础数据看板。

### 可暂缓

1. 自动爬取官网政策。
2. 扫描件 OCR。
3. 复杂表格结构化解析。
4. 多学校对比。
5. 多城市政策扩展。
6. 企业政策和政务服务真实接入。
7. LangGraph 正式接入。

## 十五、推荐开发顺序

实际开发时建议按以下顺序推进：

```text
1. 基础工程
2. 数据库模型
3. 文件上传
4. PDF 解析
5. 文档切片
6. 向量入库
7. RAG 检索
8. 政策问答
9. 出处引用
10. 前端问答页
11. metadata 与政策分层
12. 附件关系
13. 记忆层
14. 条件追问
15. 资格判断
16. 材料清单
17. 办理流程
18. 管理后台
19. 热门问题看板
20. Demo 打磨
```

## 十六、稳定演示脚本

### 脚本一：管理员构建知识库

1. 管理员进入后台。
2. 上传《奖学金评定办法》。
3. 填写政策类别、发布部门、生效时间。
4. 上传《转专业通知》。
5. 上传《附件 1：转专业申请表》并绑定主文件。
6. 系统解析文件。
7. 系统显示知识库构建完成。

展示重点：

- 平台不是静态问答，而是可维护政策知识库。
- 政策有层级、时间、适用范围和附件关系。

### 脚本二：学生咨询奖学金

1. 学生进入政策服务工作台。
2. 输入：“我挂过一门课，还能申请奖学金吗？”
3. 系统检索奖学金政策。
4. 系统回答结论。
5. 系统展示政策出处和风险提示。

展示重点：

- 自然语言问答。
- 政策依据可追溯。
- 不确定问题有风险提示。

### 脚本三：学生判断转专业资格

1. 学生输入：“我大一绩点 3.4，想转计算机专业，可以吗？”
2. 系统读取短期记忆和长期记忆。
3. 系统检索学校转专业政策和学院细则。
4. 系统追问：“是否有挂科记录？”
5. 学生回答：“没有挂科。”
6. 系统追问或确认是否有处分记录。
7. 系统输出资格判断。
8. 系统生成材料清单。
9. 系统指向《附件 1：转专业申请表》。
10. 系统生成办理流程。

展示重点：

- Agent 主动追问。
- 事项记忆记录缺失条件。
- 学校政策、学院政策和附件可联合检索。
- 输出从“答案”升级为“办事路径”。

## 十七、下一步建议

建议下一步优先做三件事：

1. **确定首批政策文件**
   先准备 3 到 5 份能稳定解析的西电政策文件。

2. **确定数据库表设计**
   先把政策、chunk、附件、关系、记忆、会话这几类表定下来。

3. **优先开发最小闭环**
   不要一开始做所有页面，先跑通“上传 PDF → 解析 → 入库 → 检索 → 问答 → 引用”。

最小闭环跑通后，再叠加 Agent 追问、资格判断、材料清单、办理流程和管理看板。
